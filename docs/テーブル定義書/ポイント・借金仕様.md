# ポイント・借金仕様

## イベントごとのリセット
ポイントおよび借金はイベント単位で管理する。イベントが変わると両方とも `0` からリスタートする（`events.initial_points` を基準値とする）。

## 現在ポイントの集計
```sql
現在ポイント = events.initial_points
             + SUM(ph.change_amount)
               FROM point_history ph
               WHERE ph.user_id = ? AND ph.event_id = ?
```

## 現在借金額の集計
```sql
借金総額 = SUM(dh.change_amount)
           FROM debt_history dh
           WHERE dh.user_id = ? AND dh.event_id = ?
```

## スケール考察
- 想定規模: ユーザー数十〜100人、イベントあたりゲーム数十本
- `point_history` / `debt_history` の最大行数 ≒ 100ユーザー × 500ゲーム = 50,000行
- `(user_id, event_id)` 複合インデックスにより `SUM` は数ms以内で完了
- このスケールでは集計コストは実用上問題ない

## イベントの初期ポイント設定
`events.initial_points` にイベントごとの初期ポイントを設定できる（デフォルト: **10,000pt**）。

## 倍率計算（パリミュチュエル方式）
競馬などで用いられるパリミュチュエル（pari-mutuel）方式を採用する。集計単位は **`bets.selected_symbols` の組み合わせ文字列** とし、`single` / 複数方式を問わず同一ロジックで計算する。

```
組み合わせの倍率 = 総賭けポイント合計 ÷ その組み合わせへの賭けポイント合計
```

- 賭けが多く集まった（人気の）組み合わせ → 倍率が低くなる
- 賭けが少ない（不人気の）組み合わせ → 倍率が高くなる

**例（single）**: 総賭けポイント 1,000pt、`"A"` に 800pt・`"B"` に 200pt が賭けられた場合
```
"A" の倍率 = 1,000 ÷ 800 = 1.25倍
"B" の倍率 = 1,000 ÷ 200 = 5.00倍
```

**例（multi_ordered, 3連単）**: 総賭けポイント 1,000pt、`"BDE"` に 600pt・`"BED"` に 400pt が賭けられた場合
```
"BDE" の倍率 = 1,000 ÷ 600 = 1.67倍
"BED" の倍率 = 1,000 ÷ 400 = 2.50倍
```

当選時の獲得ポイント = `賭けたポイント × 倍率`（小数点以下切り捨て）

## 借金ルール
- 賭け時に借金オプションを有効にした場合、ポイントは変動せず `debt_history` に `+amount` を記録する
- 借金オプションが無効の場合、`point_history` に `-amount` を記録する（集計ポイントが不足する場合はエラー）
- 当選時の獲得ポイントは `point_history` に `+獲得ポイント` を記録する（借金との自動相殺なし）
- 現時点では返済手段を用意しない。将来的には管理者による手動調整エンドポイント（例: `PATCH /api/users/:id/debt`）の追加を想定する。

## ゲーム結果修正
ゲーム結果を修正する場合、以下の手順で `point_history` を置き換える（冪等な操作）。
1. `DELETE FROM point_history WHERE game_id = ? AND reason = 'game_result'`
2. 新しい結果で当選ユーザーの獲得ポイントを再計算
3. `INSERT INTO point_history ...` で新レコードを追加

`debt_history` は賭け時点で確定するためゲーム結果修正の影響を受けない。

# テーブル定義書 - 概要

## データベース: PostgreSQL

## テーブル一覧

| テーブル名 | 説明 |
|-----------|------|
| `events` | ゲームをまとめるイベント |
| `games` | 賭けの対象となる個々のゲーム |
| `bet_options` | ゲームに設定された賭け項目 |
| `users` | Discordユーザー |
| `bets` | ユーザーの賭けレコード |
| `point_history` | ポイントの増減履歴（現在のポイントはここから集計） |
| `debt_history` | 借金の増減履歴（現在の借金額はここから集計） |

## ER 図（概略）

```
events ──── 1:N ──► games ──── 1:N ──► bet_options
  │                   │
  │                   │ 1:N
  │                   ▼
  │                bets
  │                   │ N:1
  │                   ▼
  │                users
  │                   │
  ├──── 1:N ──────────┤◄── point_history (event_id FK)
  │                   │
  └──── 1:N ──────────┘◄── debt_history  (event_id FK)
```

**備考**: `bets.selected_symbols` は `bet_options.symbol` を文字列結合したものだが、FK 制約は持たない（API 層で妥当性を検証）。
